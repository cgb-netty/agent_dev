<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bugbycode.mapper.table.TableMapper">
	
	<update id="initHostTable">
		<![CDATA[
			CREATE TABLE IF NOT EXISTS `host` (
				`id` INTEGER PRIMARY KEY AUTOINCREMENT,
				`host` VARCHAR(255),
				`forward` TINYINT(4),
				`result` TINYINT(4),
				`datetime` datetime
			)
		]]>
	</update>
	
</mapper>